<?php declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | __PACKAGE_STUDLY__ configuration
    |--------------------------------------------------------------------------
    |
    | Add your package's configuration keys here. Consumers publish this file
    | via `php artisan vendor:publish --tag=__PACKAGE__-config`.
    |
    */
];
